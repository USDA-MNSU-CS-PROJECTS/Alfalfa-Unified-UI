# assets/

Static documentation assets for the unified UI.

- `unified_ui_preview.png` — dashboard screenshot embedded in the top-level `README.md`.

Add new images here and reference them with relative paths from the markdown
files that need them (e.g. `assets/your-image.png`).
