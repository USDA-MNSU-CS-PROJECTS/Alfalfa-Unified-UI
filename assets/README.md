# assets/

Static assets for the unified UI (logos, icons, screenshots).

This folder is currently a placeholder. Drop image files here and reference
them from `app.py` via Gradio's `gr.Image` or by serving them through
`gr.Blocks`'s static file support if needed later.
